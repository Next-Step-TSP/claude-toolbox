# Shell scripts and build inputs must stay LF — they are COPY'd into a Linux
# image (the Dockerfile also strips CRLF defensively, but don't rely on it).
* text=auto
*.sh text eol=lf
Dockerfile text eol=lf
*.yml text eol=lf
*.xml text eol=lf
*.ps1 text eol=crlf
