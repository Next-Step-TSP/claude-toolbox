# CLAUDE.md — claude-toolbox (ARCHIVED)

**This repo is archived. Do not make changes here.**

Everything moved to `claude-ssh-solutions`
(<https://github.com/Next-Step-TSP/claude-ssh-solutions>). If you were sent here by a stale link,
docstring, or an old local clone, go there instead — including for the Dockerfile, the Unraid
runbook, and the GHCR publish workflow.

The live deployment is unaffected: the image tag
(`ghcr.io/next-step-tsp/claude-toolbox:latest`), the Unraid appdata paths
(`/mnt/user/appdata/claude-toolbox/…`) and the tailnet hostname (`claude-code`) all stayed the same.
Only the source repo moved.

See `README.md` for the full mapping of what went where, and for four bugs that were live in this
repo's assets and fixed during the migration.
